// App configuration — kept in code (no .env files).
// Edit these values directly to point the app at your backend / EmailJS account.

// Backend API base URL. Leave empty ("") to run in offline/localStorage mode.


// Backend API base URL
export const API_URL = "https://mern-backend-2q7n.onrender.com";

// EmailJS — sign up at https://www.emailjs.com, create a service + template,
// then paste the IDs below. Leave empty to disable order confirmation emails.
export const EMAILJS_SERVICE_ID = "";
export const EMAILJS_TEMPLATE_ID = "";
export const EMAILJS_PUBLIC_KEY = "";
